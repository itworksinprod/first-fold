{
  "name": "first-fold",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A finite morning technology newspaper: one consequential story per desk, delivered at 6:00 AM Eastern.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "pretest": "npm run build",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
